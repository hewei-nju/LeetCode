#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    int reverse(int x) {
        if (x == INT32_MIN) {
            return 0;
        } else {
            if (x < 0) {
                printf("-");
                stringstream s;
                s << -1 * x;
                string s = s.str();
            
            }
        }
    }
};