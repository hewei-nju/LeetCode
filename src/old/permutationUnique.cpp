class Solution {
public:
    vector<vector<int>> permuteUnique(vector<int>& nums) {
        vector<int> res;
        if (nums.empty()) {
            return res;
        }
        vector<int> res(nums.size(), -11);
    }
    vector<vector<int>> permutation(vector<int> &nums, vector<int>&res) {
        for (int i = 0; i <= nums.size() - 1; ++i) {
            vec
        }
    }
};


////  ! 