class Solution {
public:
    vector<string> subdomainVisits(vector<string>& cpdomains) {
        unordered_map<string, int> domainsMap;
        for (auto str: cpdomains) {
            int cnt = str.substr(0, str.find(' '));

        }
    }
};